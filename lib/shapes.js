function Shape(color) {
this.color = color;
}

function Circle(color, render){
    this.color = color;
    this.render = render;
}


function Triangle()

function Square()

// classes for each shape
// all shapes should have render function and set color function
// set color mehtod in the parent class